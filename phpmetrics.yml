# to run phpmetrics use "php -d memory_limit=1024M vendor/bin/phpmetrics --config=phpmetrics.yml . "
phpmetrics:
  path:
    extensions: php
    exclude: vendor|temp|log
  logging:
    report:
      html: ./.phpmetrics/phpmetrics-actual.html
